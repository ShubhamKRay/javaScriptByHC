//+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
// Stack(primitive) and Heap(Non-primitive) Memory
//++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

//! Stack and Heap are two different types of memory used in programming languages to manage data.

//! Stack memory is used to store primitive data types (such as numbers, strings, booleans, null, undefined, symbols, and bigints) and function calls. It is a last-in-first-out (LIFO) data structure where the most recently added item is the first one to be removed. When a function is called, a new stack frame is created to hold the function's local variables and parameters. When the function returns, the stack frame is removed from the stack.
//! Heap memory, on the other hand, is used to store non-primitive data types (such as objects, arrays, and functions). It is a more flexible memory structure that allows for dynamic allocation and deallocation of memory. When an object is created, it is stored in the heap memory, and a reference to that object is stored in the stack memory. When the reference goes out of scope, the memory allocated for the object in the heap can be garbage collected.

//! In summary, stack memory is used for primitive data types and function calls, while heap memory is used for non-primitive data types. Understanding the difference between stack and heap memory is important for efficient memory management and performance optimization in programming languages.

let myYoutubename = "shubhkipathsala";
let anothername = myYoutubename; //! In this code snippet, we are assigning the value of myYoutubename to anothername. Since myYoutubename is a primitive data type (a string), it is stored in stack memory. When we assign myYoutubename to anothername, a copy of the value is created and stored in another location in stack memory. This means that myYoutubename and anothername are two separate variables that hold the same value, but they are stored in different locations in memory. If we change the value of anothername, it will not affect the value of myYoutubename, since they are stored in different locations in memory. For example, if we set anothername to "CodeWithShubham", then myYoutubename will still be "shubhkipathsala", and anothername will be "CodeWithShubham". This is because they are two separate variables that hold the same value, but they are stored in different locations in memory.
anothername = "CodeWithShubham"; //! In this code snippet, we are changing the value of anothername to "CodeWithShubham". Since anothername is a separate variable that holds a copy of the value of myYoutubename, changing the value of anothername will not affect the value of myYoutubename. After this assignment, myYoutubename will still be "shubhkipathsala", while anothername will be "CodeWithShubham". This is because they are two separate variables that hold the same value, but they are stored in different locations in memory. The change to anothername does not affect myYoutubename because they are stored in different locations in memory and are independent of each other.

console.log(myYoutubename); //! This will output "shubhkipathsala" because myYoutubename is a separate variable that holds the original value, and changing anothername does not affect it.
console.log(anothername); //! This will output "CodeWithShubham" because we have changed the value of anothername to "CodeWithShubham", and it is a separate variable that holds its own value, independent of myYoutubename.

let userOne = {
  email: "shubham@example.com",
  upi: "shubham@oksbi",
}; 
//! In this code snippet, we are creating an object userOne with two properties: email and upi. This object is stored in heap memory because it is a non-primitive data type. When we assign userOne to userTwo, we are not creating a copy of the object; instead, we are creating a reference to the same object in heap memory. This means that both userOne and userTwo point to the same object in memory. If we change a property of the object through either userOne or userTwo, it will affect both variables since they reference the same object in memory. For example, if we change the email property of the object through userTwo, it will also change the email property when accessed through userOne, because they both reference the same object in heap memory.

let userTwo = userOne; //! In this code snippet, we are assigning userOne to userTwo. Since userOne is an object (a non-primitive data type), it is stored in heap memory. When we assign userOne to userTwo, we are creating a reference to the same object in heap memory. This means that both userOne and userTwo point to the same object in memory. If we change a property of the object through either userOne or userTwo, it will affect both variables since they reference the same object in memory. For example, if we change the email property of the object through userTwo, it will also change the email property when accessed through userOne, because they both reference the same object in heap memory.
userTwo.email = "ray@google.com"; //! In this code snippet, we are changing the email property of the object that userTwo references to "
console.log(userOne.email);  //! This will output "ray@google.com"
console.log(userTwo.email);  //! This will also output "ray@google.com" because userOne and userTwo reference the same object in heap memory, and changing the email property through userTwo affects the same object that userOne references. Therefore, both userOne.email and userTwo.email will reflect the change and output "ray@google.com"
